/**
 * Localization
 */
var strings = null;
var strings_file = __dirname + '/../strings/events.yml';

// Keep track of password attempts
var password_attempts = {};

exports.login = function(arg, stage, dontwelcome, name) {
}